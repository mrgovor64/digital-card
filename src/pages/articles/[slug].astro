---
import "../../styles/global.css";

// @ts-ignore
export async function getStaticPaths(): Promise<any[]> {
  // @ts-ignore
  const articles = await Astro.glob("../../../content/articles/*.md");

  return articles.map((article: any) => {
    const slug = article.file.split("/").pop()?.replace(".md", "") || "";
    return {
      params: { slug },
      props: { article },
    };
  });
}

const { article } = Astro.props;
const { Content, frontmatter } = article;
const date = frontmatter.date
  ? new Date(frontmatter.date).toLocaleDateString("en-US", {
      year: "numeric",
      month: "long",
      day: "numeric",
    })
  : null;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <meta
      name="description"
      content={frontmatter.description || "Article by Vladimir Govorukhin"}
    />
    <title>{frontmatter.title || "Untitled"} - Vladimir Govorukhin</title>
  </head>
  <body
    class="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 text-white"
  >
    <main class="container mx-auto px-4 py-16">
      <div class="max-w-4xl mx-auto">
        <!-- Back link -->
        <div class="mb-6">
          <a
            href="/articles"
            class="inline-block text-gray-400 transition-colors duration-300 hover:text-purple-400 text-sm"
          >
            ← Back to Articles
          </a>
        </div>

        <!-- Header Section -->
        <header class="text-center mb-16 animate-fade-in">
          <h1
            class="text-5xl md:text-6xl font-bold mb-4 bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent"
          >
            {frontmatter.title || "Untitled"}
          </h1>

          <div
            class="flex flex-wrap items-center justify-center gap-3 text-gray-400 text-sm mb-4"
          >
            {date && <time>{date}</time>}
            {
              frontmatter.externalPublication && date && (
                <span class="text-gray-500">•</span>
              )
            }
            {
              frontmatter.externalPublication && (
                <span class="text-gray-400 italic">
                  Originally published on{" "}
                  {frontmatter.externalPublicationUrl ? (
                    <a
                      href={frontmatter.externalPublicationUrl}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-purple-400 transition-colors duration-300 hover:text-purple-300 underline"
                    >
                      {frontmatter.externalPublication}
                    </a>
                  ) : (
                    <span>{frontmatter.externalPublication}</span>
                  )}
                </span>
              )
            }
          </div>

          {
            frontmatter.description && (
              <p class="text-xl text-gray-400 leading-relaxed">
                {frontmatter.description}
              </p>
            )
          }
        </header>

        <!-- Article Content -->
        <article
          class="bg-white/10 backdrop-blur-lg rounded-2xl shadow-2xl p-8 md:p-12 border border-white/20 prose prose-invert prose-lg max-w-none prose-headings:text-purple-300 prose-headings:font-semibold prose-a:text-purple-400 prose-a:no-underline hover:prose-a:underline prose-code:text-purple-300 prose-code:bg-white/10 prose-code:px-1 prose-code:py-0.5 prose-code:rounded prose-pre:bg-slate-900 prose-pre:border prose-pre:border-white/20 prose-strong:text-white prose-em:text-gray-300"
        >
          <Content />
        </article>
      </div>
    </main>
  </body>
</html>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in {
    animation: fade-in 0.6s ease-out;
  }

  /* Custom prose styles for better readability */
  .prose {
    color: #e2e8f0;
  }

  .prose p {
    margin-top: 1.25em;
    margin-bottom: 1.25em;
    line-height: 1.75;
  }

  .prose h1,
  .prose h2,
  .prose h3,
  .prose h4 {
    margin-top: 2em;
    margin-bottom: 1em;
    font-weight: 600;
  }

  .prose h2 {
    font-size: 1.875em;
  }

  .prose h3 {
    font-size: 1.5em;
  }

  .prose ul,
  .prose ol {
    margin-top: 1.25em;
    margin-bottom: 1.25em;
    padding-left: 1.625em;
  }

  .prose li {
    margin-top: 0.5em;
    margin-bottom: 0.5em;
  }

  .prose blockquote {
    margin-top: 1.6em;
    margin-bottom: 1.6em;
    padding-left: 1em;
    border-left: 0.25em solid rgba(196, 181, 253, 0.5);
    color: #cbd5e1;
  }

  .prose hr {
    margin-top: 3em;
    margin-bottom: 3em;
    border-color: rgba(255, 255, 255, 0.1);
  }

  .prose img {
    margin-top: 2em;
    margin-bottom: 2em;
    border-radius: 0.5rem;
  }
</style>
